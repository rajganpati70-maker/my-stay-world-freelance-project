{
  "name": "anyrenting-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android (Capacitor) wrapper for the AnyRenting PWA",
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
